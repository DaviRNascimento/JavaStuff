public class ShortHandIfElse {
    // known as Ternary Operator;
    public static void ternaryOperator(byte time) {
        String result = (time >= 18) ? "Good Night" : "Good Day";
        System.out.print(result);
    }
}
